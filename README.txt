Name: Alexander Lokhanov
To use this code and compile it, use "gcc parser.c"
Once it is compiled, use "./a.out " followed by a file name (must be .txt!)
Output will be displayed on screen as well as in outParse in the same directory.